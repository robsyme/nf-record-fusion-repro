manifest {
    name            = 'nf-record-fusion-repro'
    description      = 'Minimal reproducer: record Path field not staged under Fusion'
    nextflowVersion  = '>=26.04.0'
}

// NOTE: the typed syntax needs the strict v2 parser, selected by NXF_SYNTAX_PARSER=v2
// (read before this config, so it cannot be set here) or the pipeline's v2 toggle.
